[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  cp = cherry-pick
  hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
  type = cat-file -t
  dump = cat-file -p
  publish = !/usr/bin/git-publish
[core]
  editor = vim -p
